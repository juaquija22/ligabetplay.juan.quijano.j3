


import json 
import utils.corefiles as cor
import utils. screencontrollers as sc
import utils. validatedata as vd
import utils.corefiles as cf
from config import DB_PATH
import random


'''def jugadores():
    sc.limpiar_pantalla
    ideq = random.randint(1023, 9876)
    nombrejug = vd.validatetext('Nombre del jugador :')
    posicion = vd.validatetext('posicion del jugador :')
    dorsal = vd.validateInt('numero del dorsal :')
    id = vd.validateInt('Id del equipo al que pertenece :')
    eq = {
        ideq:{
            "nombre":teamname,
            "fundacion":fecha,
            "pais":origen,
            "idliga":id
        }
    }


    if not cf.updateJson(eq,["equipos"]):
            print("equipo agregado exitosamente ")
            sc.pausar_pantalla
    else:
        print("No se pudo agregar el equipo ")
        sc.pausar_pantalla'''